# 项目索引

> 门禁：本文件只写最新且已确认的可运行状态；写入新条目时删除上一条。历史交付细节见 `docs/迭代升级/执行事实.yaml`。

## 最新状态

**当前版本基线**：产品版本 `0.5.1`。**V5.1 桌面体验补丁已完成源码侧实施与离线验收**：408 用例全绿、compileall/git diff --check 通过。V5.0 视频链接源获取（大版本，基线 `0.4.2`）已完成 P1–P5 全部验收（CP1–CP5），P6 离线部分完成，便携发行包重建待单独授权。

V5.1 补丁要点：
- **缓存恢复修复**：manifest `stages.render` 仅记录 markdown 路径（docx/pdf 缺失），导致 `cached_result` 返回 None，移除视频重加后状态/token/文件打开权限丢失。修复后 `cached_result` 在 manifest 路径缺失时扫描 `output/<video_id>/` 目录补全 `.md/.docx/.pdf`；链接源 `add_url` 下载完成后也调用 `cached_result` 恢复已完成状态。
- **Token 列宽度修复**：窗口默认/最小宽度 980→1040；列宽大幅调整（name 300→170、tokens 155→225），Token 栏完整可见。
- **日志修复**：移除独立 status 标签（与日志第一行重复导致三行重叠）；日志改为 2 行固定高度 + 垂直滚动条，追加式记录（保留最近 200 行可滚动回看）。

## 文档链路

- [AI 执行入口](AI执行入口.md) — Agent 干活入口：约束、待执行事项、直接行动步骤。
- [当前架构升级状态](迭代升级/当前架构升级状态.yaml) — 只含 active phase、blocker、next 与验证摘要。
- [模块边界](architecture/module-boundaries.yaml) / [步骤目录](architecture/pipeline-steps.yaml) / [故障索引](diagnostics/problem-index.yaml) — 定位问题的机器可校验入口。
- [V4.0 架构合同](迭代升级/V4.0架构合同.md) — 当前主要架构方案，未来要进行架构更迭时参考和修改。
- [执行事实](迭代升级/执行事实.yaml) — 完成历史与审计证据；不要默认通读。
- [用户版本时间线](迭代升级/迭代记录与问题.md) — 只保存用户批准的问题和最终方案。
- [便携打包方案](../packing/scheme/打包方案.md#后续迭代的增量打包约束) — 后续迭代默认复用 `models/` 与 `tools/`，仅传输非模型、非工具更新；复用前必须校验完整性。
- [交互思考](迭代升级/交互思考/)升级时的思考升级时的思考、问题、解决方案等；在和用户交互思考进行写入，不作为升级和执行的权威文件。
- [项目文档](项目文档) — 存放项目的设计、架构、开发、业务等文档；用户明确再进行彻底更新；
- [screenshot](screenshot) — 存放项目的截图、视频等可视化文档；用户特定指定时进行读取；

## 重点脚本索引

- [测试脚本代码](../tests) — 存放测试相关脚本；包括冒烟测试，链路升级测试等。
- [临时文件及脚本](../tmp) — 存放不影响项目运行的agent中间产物，文档、脚本等。

## 交接入口

[V5.0 视频链接源获取升级执行计划](迭代升级/V5.0视频链接源获取升级执行计划.md)（P1–P6 恢复点 CP1–CP6 已标注验收状态）。本轮未重新下载模型权重；`tools/yt-dlp.exe`（2026.07.04）已就位；便携发行包重建、真实长视频重跑、云端请求仍需单独授权。
