# Git 上传边界：未命中忽略规则的源码、tests/、docs/、release 文档与清单可以提交。
# 永不上传：密钥和本机私有设置
.env
.env.*

# 不上传：根目录用户数据、模型、工具和历史打包产物
/Resource/
/视频/
/models/
/workspace/
/output/
/packing/
/tools/

# 提交 release 文档/manifests/checksums；ZIP 不进 Git，审核后上传到同版本 GitHub Release
/release/output/*
!/release/output/.gitkeep

# 不上传：构建缓存和临时文件
/build/
/tmp/
/tmp_*/
*.log
*.pid


# 不上传：工具本地会话和诊断缓存
/.codely-cli/
/mem-log/

# 不上传：Python 环境和缓存
/.venv/
/venv/
*.egg-info/
*.py[cod]
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 不上传：Node.js 依赖和缓存
node_modules/
.npm-cache/

# 不上传：编辑器和操作系统临时文件
.idea/
.vscode/
.DS_Store
Thumbs.db
Desktop.ini
~$*

# 不上传：其他不必要的文件
AGENTS.local.md
CODELY.md
