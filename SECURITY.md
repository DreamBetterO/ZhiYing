# 安全策略

## 支持范围

当前维护分支为 `1.0.x`。开发快照和历史便携包仅用于迁移或测试，不承诺安全更新。

## 报告漏洞

请不要在公开 Issue 中披露 API Key、可利用细节、用户视频、诊断报告中的个人路径或其他敏感数据。

公开仓库启用后，优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。报告应包含：

- 受影响版本和 Windows 环境；
- 最小复现步骤；
- 影响范围；
- 已做的本地缓解措施；
- 必要且已脱敏的日志或截图。

## 发行安全边界

- `.env` 和密钥永不进入 Git、日志或发行包；
- 核心 ZIP 必须通过秘密文件、用户数据、调试残留和随包文档门禁；
- 模型和工具只从 `release/DOWNLOADS.md` 列出的上游来源获取；
- 正式发布应附带 SHA-256；
- 商业推广或面向大量普通用户前，应补充 SBOM、第三方许可证审计和 Windows 代码签名。
